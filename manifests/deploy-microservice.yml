kind: Deployment
apiVersion: apps/v1
metadata:
    labels:
        app: solo-koldo-microservice
        stack: frontend
        source: demo
    name: solo-koldo-microservice
    namespace: microservices
spec:
    replicas: 3
    selector:
        matchLabels:
            run: solo-koldo-microservice
    template:
        metadata:
            labels:
                app: solo-koldo-microservice
                run: solo-koldo-microservice
                source: demo
        spec:
            containers:
                -   image: 219099013464.dkr.ecr.us-west-2.amazonaws.com/solo-koldo-microservice
                    name: solo-koldo-microservice
                    ports:
                        -   name: web
                            containerPort: 8080
---

kind: Service
apiVersion: v1
metadata:
    labels:
        app: solo-koldo-microservice-service
    name: solo-koldo-microservice-service
    namespace: microservices
spec:
    selector:
        source: demo
        app: solo-koldo-microservice
    ports:
        -   name: web
            protocol: TCP
            port: 30201
            targetPort: 8080

---

apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
    annotations:
        nginx.ingress.kubernetes.io/rewrite-target: /$2
    name: solo-koldo-ingress
    namespace: microservices
spec:
    rules:
        -   http:
                paths:
                    -   path: /solo-koldo(/|$)(.*)
                        backend:
                            serviceName: solo-koldo-microservice-service
                            servicePort: 30201


